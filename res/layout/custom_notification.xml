<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip"
    android:background="#7777">

    <ImageView
        android:id="@+id/imagen_notificacion"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/ic_notification"/>

    <TextView
        android:id="@+id/contenido_notificacion"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@+id/imagen_notificacion"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:textSize="12sp"
        android:textColor="#FFF"
        android:maxLines="2"/>

    <TextView
        android:id="@+id/nombre_notificacion"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/contenido_notificacion"
        android:layout_alignParentTop="true"
        android:gravity="center_vertical"
        android:textColor="#FFF"
        android:text="@string/cambio_notificacion"
        android:textSize="16sp" />

</RelativeLayout>